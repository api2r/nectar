# Check that an object is an httr2_response

Check that an object is an httr2_response

## Usage

``` r
check_httr2_response(x, call = rlang::caller_env())
```

## Arguments

- x:

  An object to check.

- call:

  The calling environment for error reporting.

## Value

`x`, invisibly, if it passes the check.
